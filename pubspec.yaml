name: quetzal_marketplace

description: A marketplace for vendors and products.

publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.4
  provider: ^6.0.0
  # Add any other dependencies here

flutter:
  uses-material-design: true
